# Copy to .env.local (gitignored). Discord posting is dev-only and is
# stripped from production builds — these vars never ship to GitHub Pages.

# Forum-channel webhook URL. Each post creates a new thread.
VITE_DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/REPLACE_ME
